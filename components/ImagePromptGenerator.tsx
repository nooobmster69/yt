import * as React from 'react';

// This component is deprecated. Its functionality has been merged into ImagePromptCombiner.tsx
// It now returns null to be effectively removed from the UI without breaking imports.
export const ImagePromptGenerator = () => {
    return null;
};
